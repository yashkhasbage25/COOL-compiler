(* Illustrates inheritance *)

(* class A {
    a : Int;
};

class B inherits A {
    b : Int;
};*)

class Main {
    a : Int;
    b: Int;
    main() : Int {
        {
            while a < 10 loop {
                a <- a + 1;
                b <- 2 * b;
            } pool;
        0;
        }

    };
};
